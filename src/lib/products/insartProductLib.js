const Products = require("../../models/Products");

const insertProductLib = async (query) => {
console.log(query)
//   const cursor = await Products.insertMany(query);
//   return cursor;
};

module.exports = insertProductLib;
